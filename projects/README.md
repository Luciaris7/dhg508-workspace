# Projects

Your own work lives here. Each project gets its own folder, copied from the
template:

```bash
cp -R templates/project_template projects/my_first_project
```

Do not use `templates/project_template/` as a working project. Copy it first,
then work inside the copied project directory.
