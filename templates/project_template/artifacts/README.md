# Artifacts

Large generated outputs and local rebuild products go here.

Examples:

- OCR page-level JSON dumps
- search indexes and embeddings
- cache directories
- live SQLite, DuckDB, or other database files
- bulk converted assets

This directory is ignored by git except for this README. Record what each
artifact is, where it came from, and how to rebuild it.
